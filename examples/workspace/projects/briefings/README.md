# briefings

Where the research briefing files what it writes, one document per topic. A re-run on the same topic creates a
new version of the same document rather than a second file, so the Library's diff shows what changed this week.
